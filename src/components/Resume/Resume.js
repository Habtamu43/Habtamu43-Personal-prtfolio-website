import React from "react";
import './Resume.css';

const Resume = () => {
  return (
    <div className="i-floatingdiv position-fixed">
      {/* Content for the floating div (if needed) */}
    </div>
  );
};

export default Resume;
