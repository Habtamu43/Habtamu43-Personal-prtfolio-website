// src/index.js
import React from 'react';
import ReactDOM from 'react-dom';
import APP from './App';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <APP /> {/* Render the Football component */}
  </React.StrictMode>
);
